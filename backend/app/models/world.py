"""World database model placeholders."""

# For V1 we defer implementing a persistent world model.  Future versions
# should define SQLAlchemy models here to store geography, factions, NPCs,
# locations and other world data.