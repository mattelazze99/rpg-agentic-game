# Frontend placeholder

The backend in this bundle is complete enough to run locally.
A frontend can be attached later against these endpoints:

- `GET /`
- `GET /health`
- `POST /session/`
- `GET /session/{session_id}`
- `GET /world/{session_id}`
